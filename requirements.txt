# RLcapstone — Python dependencies (Python 3.12)
#
# PyTorch is installed SEPARATELY so you can pick the right build. Install it
# first, then this file (ultralytics will reuse the torch you already have):
#
#   GPU (NVIDIA, incl. Blackwell / RTX 50-series):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   CPU-only (inference, export, and most scripts work fine):
#     pip install torch torchvision
#
#   then:  pip install -r requirements.txt
#
# Versions are left unpinned for portability; the project was developed and
# tested with the versions noted in comments below.

# --- core ---
numpy
pandas
pillow

# --- training / computer vision ---
ultralytics          # YOLO11s-cls transfer learning (pulls torch/torchvision, opencv); tested 8.4.92

# --- model export: PyTorch -> ONNX -> TensorFlow Lite ---
onnx
onnxruntime
onnx2tf              # ONNX -> TFLite conversion
tensorflow           # required by onnx2tf; tested 2.21
ai-edge-litert       # TFLite interpreter used for export parity checks

# --- datasets & signals ---
kaggle               # dataset downloads (C-NMC, PAD-UFES-20, Aria, Acevedo)
wfdb                 # MIT-BIH ECG records (ECG project)
yfinance             # daily price data (stock project)

# --- pentest sandbox (educational, localhost only) ---
flask
markupsafe

# --- capstone PDF reports ---
reportlab

# Notebooks (optional): the notebooks/ demos need `jupyterlab` — install it
# separately if you want to run them; it is intentionally not required here.
